#!/usr/bin/perl
use common::sense;

my $towatch;
$towatch = $ARGV[0] if ($ARGV[0]) ;
$towatch = "fortune" unless ($ARGV[0]);

system("clear");
while(1) {
        system("$towatch");
        my $input;
        $input = <STDIN>;
        exit 0 if $input =~ /:q/;
        system("clear");
}
